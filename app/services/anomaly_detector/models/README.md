Model artifacts are not stored in this service directory.

Runtime model files are mounted from:

  artifacts/models

Required files:
- isolation_forest_final.joblib
- scaler.joblib
- training_data.npy
- feature_names.json
