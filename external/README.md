External vulnerable applications are not stored in this repository.

For full experimental reproduction, place target applications outside the repository and pass their code path to run.sh:

  ./run.sh /path/to/target-app http://localhost:PORT

Examples used in the study included vulnerable API and LLM-oriented test applications.
